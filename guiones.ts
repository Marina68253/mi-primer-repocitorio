import * as  rls from 'readline-sync';

function dibujarGuiones(numeroGuiones : number){
    let i: number;
    let linea: string="";
    for (i= 0; i <= numeroGuiones; i++){
        linea=linea + "-";
    }
    console.log("linea");
}